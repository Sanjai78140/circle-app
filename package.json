{
  "name": "circle-match",
  "version": "1.0.0",
  "description": "A round-based matchmaking circle: quiz, lobby, reveal, tarot results, timed chat, auto-loop.",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
